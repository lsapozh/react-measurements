import React, {Component} from 'react';
import styled from 'styled-components';

export const AddNewRecord = styled.button`
  position: fixed;
  bottom: 0px;
  width: 100%;
  height: 40px;
  background-color: lightcoral;
  font-size: 15px;
  font-weight: 600;
  color: rgba(0, 0, 0, 0.8);
  border-color: lightcoral;
`;
